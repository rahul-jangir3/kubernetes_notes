apiVersion: v1
kind: Service
metadata:
  name: apache-service
  namespace: apache
spec:
  selector:
    app: apache
  ports:
    - protocol: TCP
      port: 80 # exposing port inside the cluster
      targetPort: 80 # port on which our container is running
  type: ClusterIP